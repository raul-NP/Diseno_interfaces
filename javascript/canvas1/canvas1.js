document.addEventListener('DOMContentLoaded', () => {

    const lienzo = document.getElementById('lienzo');
    const contexto = lienzo.getContext("2d");

    contexto.fillRect(10, 10, 400, 3);
    // contexto.
})